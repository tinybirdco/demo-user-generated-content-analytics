TOKEN "top_devices_endpoint_read_7442" READ

NODE endpoint
SQL >

    SELECT
      JSONExtractString(message, 'viewer_device_category') AS device,
      count(message) AS c
    FROM parse_events
    GROUP by device


